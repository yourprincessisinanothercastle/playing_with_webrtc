const LEFT = 'left';
const RIGHT = 'right';
const UP = 'up';
const DOWN = 'down';
const STILL = 'still';

const DIRECTIONS = {
    LEFT,
    RIGHT,
    UP,
    DOWN
};

const WALKING = 'walking';
const SITTING = 'sitting';

const STATES = {
    WALKING,
    SITTING,
    STILL,
};

export default {
    DIRECTIONS, 
    STATES
};
