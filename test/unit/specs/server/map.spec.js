
import Map from '../../../../src/components/server/game/worker/map/Map';

describe('Calculator', () => {

  it('should calculate zero for invalid y value', () => {
    expect(true).to.equal(true);
  });

});
